import React from 'react';

const FavoriteLayout = ({ children }: { children: React.ReactNode }) => {
  return <div className="mx-auto rounded-lg">{children}</div>;
};

export default FavoriteLayout;
