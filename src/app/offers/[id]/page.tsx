'use client'

const OffersRoute = ({ params }: { params: { id: string } }) => {

    

    return (
        <>
            Offers: {params.id}
        </>
    )
}

export default OffersRoute