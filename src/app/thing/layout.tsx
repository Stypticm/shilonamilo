const ThingLayout = ({ children }: { children: React.ReactNode }) => {

    return (
        <div className='mt-10 h-full w-full'>
            {children}
        </div>
    )
}

export default ThingLayout